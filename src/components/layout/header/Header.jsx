import React from "react";

// function Header () {}
// 위 함수로 작성해줘도 무관함! 

// header component
const Header = () => {
  // 대문자로 적어줘야 컴포넌트로 인식함
  return (
    <header>
      <h1>dashboard</h1>
    </header>
  )
};

export default Header;
// export해줘야 이 컴포넌트 불러올 수 있다!